# =============================================================================
# 🌐 프로젝트 네임스페이스 정의
# =============================================================================
# 📝 설명: web3-community 애플리케이션을 위한 격리된 공간 생성
# 🎯 목적: 리소스 충돌 방지, 권한 관리, 환경 분리
# 🔄 실행 순서: #1 (가장 먼저 실행해야 함)
# ⚠️  주의사항: 삭제 시 모든 리소스 함께 삭제됨
# 🌟 실무 팁: production/staging/development으로 나누어 관리
# 📖 참고: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
# =============================================================================

apiVersion: v1                      # 쿠버네티스 API 버전
kind: Namespace                     # 리소스 종류: 네임스페이스

metadata:
  name: web3-community              # 네임스페이스 이름 (전역 고유)
  labels:                          # 라벨: 리소스 식별 및 필터링용
    environment: local              # 환경 구분 (local/dev/staging/prod)
    project: web3-community         # 프로젝트명
    managed-by: manual             # 관리 방식 (manual/gitops/helm)
    team: platform                 # 담당 팀
  
  annotations:                     # 어노테이션: 추가 메타데이터
    description: "Web3 Community Platform - Local Development Environment"
    created-by: "developer"
    created-date: "2024-02-03"
    contact: "dev-team@web3-community.com"
    # 네임스페이스 수준 리소스 쿼터 설정 (선택사항)
    # quota.kubernetes.io/requests.cpu: "2"
    # quota.kubernetes.io/requests.memory: "4Gi"
    # quota.kubernetes.io/pods: "20"
    # quota.kubernetes.io/services: "10"
    # quota.kubernetes.io/secrets: "10"

spec:
  # 네임스페이스는 spec 필드가 거의 없음 (대부분 metadata에 설정)
  finalizers: []                    # 네임스페이스 삭제 시 실행될 정리 작업

# =============================================================================
# 🔍 설명 및 사용법
# =============================================================================
# 
# 왜 네임스페이스가 필요한가요?
# 1. 리소스 격리: 다른 프로젝트와 리소스 이름 충돌 방지
# 2. 권한 관리: 네임스페이스별로 RBAC 권한 설정 가능
# 3. 환경 분리: dev, staging, prod 환경 논리적 분리
# 4. 리소스 쿼터: 네임스페이스별 리소스 사용량 제한
#
# 실행 명령어:
# kubectl apply -f 01-namespace.yaml
#
# 확인 명령어:
# kubectl get namespace web3-community
# kubectl get namespaces
# kubectl describe namespace web3-community
#
# 삭제 명령어 (주의: 모든 리소스 함께 삭제!):
# kubectl delete namespace web3-community
#
# 실무 팁:
# - 개발 초기에는 default 네임스페이스도 사용 가능하지만, 
#   실제 운영에서는 반드시 별도 네임스페이스 사용
# - ResourceQuota를 설정하여 무한정 리소스 사용 방지
# - NetworkPolicy와 함께 사용하면 네트워크 격리도 가능
# =============================================================================