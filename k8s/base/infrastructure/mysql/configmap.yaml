apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-initdb-config
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS web3_auth;
    CREATE DATABASE IF NOT EXISTS web3_user;
    CREATE DATABASE IF NOT EXISTS web3_post;
    CREATE DATABASE IF NOT EXISTS web3_comment;
